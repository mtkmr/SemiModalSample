platform :ios, '13.0'

target 'SemiModalSample' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for SemiModalSample
  pod 'IQKeyboardManager'

end
